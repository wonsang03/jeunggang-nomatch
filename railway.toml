# 반드시 Root Directory 비우기 (저장소 루트)
# Settings → Root Directory = (empty)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
